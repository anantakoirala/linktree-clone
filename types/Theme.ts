export type Theme = {
  id: number;
  color: string;
  text: string;
  name: string;
  shopBox: string;
  hover: string;
  linkStyle: string;
  embosedBox: boolean;
  embosedBoxColor: string;
  boxColor: string;
  tabColor: string;
};
